# Railway Environment Variables Template
# Copy these to your Railway project's environment variables

# Database (Railway PostgreSQL Plugin)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Redis (Railway Redis Plugin - optional)
REDIS_URL=${{Redis.REDIS_URL}}

# API Configuration
API_HOST=0.0.0.0
API_PORT=${{PORT}}
LOG_LEVEL=INFO

# CORS (set to your frontend domain)
CORS_ALLOW_ORIGINS=https://your-frontend-domain.com

# Optional: Scraper settings (if running scraper on Railway)
SCRAPER_USER_AGENT=UFC-Pokedex-Scraper/0.1
SCRAPER_DELAY_SECONDS=1.5
SCRAPER_CONCURRENT_REQUESTS=4
