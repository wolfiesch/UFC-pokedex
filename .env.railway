# Railway Environment Variables
# Copy these to your Railway project dashboard: https://railway.app/dashboard
# If you debug locally, ensure `$HOME/.local/bin` is on your PATH so the `uv` installer matches Railway's runtime.

# Database (auto-configured when you add PostgreSQL plugin)
# DATABASE_URL=postgresql://postgres:***@***.railway.app:5432/railway

# Redis (auto-configured when you add Redis plugin)
# REDIS_URL=redis://***.railway.internal:6379

# CORS - REQUIRED: Set your Vercel frontend URL
CORS_ALLOW_ORIGINS=https://ufc-pokedex.vercel.app

# Logging
LOG_LEVEL=INFO

# API Configuration (Railway auto-sets PORT)
API_HOST=0.0.0.0
# PORT is automatically set by Railway - don't override

# Note: DATABASE_URL and REDIS_URL are automatically set by Railway plugins
# You only need to manually set CORS_ALLOW_ORIGINS to your Vercel URL
